#ifndef PULLSH_H_INCLUDED
#define PULLSH_H_INCLUDED

#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

vector<string> lineToVec(const string &);
string normalisation(string &);


#endif // PULLSH_H_INCLUDED
